<!-- Добавляем запись времени начала выполнения метода -->
        <setProperty propertyName="methodStartTime">
            <simple>${date:now:yyyy-MM-dd HH:mm:ss.SSS}</simple>
        </setProperty>
        <setProperty propertyName="methodStartTimestamp">
            <simple>${date:now:epoch}</simple>
        </setProperty>




 <!-- Логирование времени выполнения -->
            <setProperty propertyName="methodEndTime">
                <simple>${date:now:yyyy-MM-dd HH:mm:ss.SSS}</simple>
            </setProperty>
            <setProperty propertyName="methodEndTimestamp">
                <simple>${date:now:epoch}</simple>
            </setProperty>
            <setProperty propertyName="executionTimeMs">
                <groovy>
                    (exchange.properties.methodEndTimestamp - exchange.properties.methodStartTimestamp) * 1000
                </groovy>
            </setProperty>
            <log message="Method execution time: Start=${exchangeProperty.methodStartTime}, End=${exchangeProperty.methodEndTime}, Duration=${exchangeProperty.executionTimeMs} ms" loggingLevel="INFO" loggerRef="ru.seventech.esb.technoprom.createPackage.v2.timing"/>
            
            <to uri="log:ru.seventech.esb.technoprom.createPackage.v2.end?showAll=false"/>
