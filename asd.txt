private volatile ObjectNode token;  // volatile для visibility между потоками
Использовать атомарные операции для обновления токена:

java
private void refresh() {
    if (token == null || isTokenExpired()) {
        initialize();
    }
}

private boolean isTokenExpired() {
    long currentTime = System.currentTimeMillis() / 1000;
    return token.get("timestamp").asLong() + token.get("expires_in").asLong() < currentTime + timeShift;
}
