private final ReentrantLock lock = new ReentrantLock(true); // fair = true

public void get() {
    lock.lock();
    try {
        // Критическая секция
    } finally {
        lock.unlock();
    }
}
